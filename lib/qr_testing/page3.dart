//open Web URL from Scanned QR Code

void openURLFromQRCode(String url) async {
  // if (await canLaunch(url)) {
  //   await launch(url);
  // } else {
  //   throw "Could not launch $url";
  // }
}
